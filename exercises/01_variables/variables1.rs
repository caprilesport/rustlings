// variables1.rs
//
// Make me compile!
//
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a
// hint.

fn main() {
    let x = 5;

    let y = 4;

    let mut z = 3;
    z = z + y;
    println!("x has the value {}, {}, {}", x, y, z);
}
